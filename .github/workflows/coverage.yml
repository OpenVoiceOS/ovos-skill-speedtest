name: Code Coverage

on:
  pull_request:
    branches: [dev]
  workflow_dispatch:

jobs:
  coverage:
    uses: OpenVoiceOS/gh-automations/.github/workflows/coverage.yml@dev
    secrets: inherit
    with:
      python_version: '3.11'
      coverage_source: 'ovos_skill_speedtest'
      test_path: 'test/unittests/'
      system_deps: 'swig libfann-dev'
      test_extras: 'test'
      min_coverage: 0
